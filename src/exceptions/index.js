const userExceptions = require('./user.exceptions');
const authExceptions = require('./auth.exceptions');
const productExceptions = require('./product.exceptions');

module.exports = { userExceptions, authExceptions, productExceptions };
